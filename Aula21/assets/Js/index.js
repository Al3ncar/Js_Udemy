function Escop(){

     const form = document.querySelector(`form`)
     const result = document.querySelector(`result`)

     function meuEscop(e){
        e.preventDefault()
        const peso = form.querySelector(`peso`)
        const altura = form.querySelector(`altura`)
        result.push{
            
        }
     }
     form.addEventListener(`submit`, meuEscop())
}
Escop()